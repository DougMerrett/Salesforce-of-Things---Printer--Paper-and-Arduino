The Java Proxy has its own ReadMe.txt, please read that for information.

To make the Sketch work, just use Arduino IDE v1.0 (or higher) and run it.  There is a diagram
on how the scales were built in the HowTo.pdf